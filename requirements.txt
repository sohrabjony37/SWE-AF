# AgentField SWE Pipeline — Runtime Dependencies
#
# Install: python -m pip install -r requirements.txt

agentfield>=0.1.9
pydantic>=2.0
claude-agent-sdk==0.1.20
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
